export default function EmailTemplate({ firstName }) {
    return (
        <div>
            <h1>Hello, {firstName}!</h1>
            <p>This feature is still being worked on! Contact kieranc0808@gmail.com</p>
        </div>
    );
}