export default async function BookChamber({ userData }) {
    return (
        <div className="mt-10 grid grid-cols-4 justify-items-stretch gap-10 text-violet-950 dark:text-violet-100">
            <h1 className="text-center col-span-full">Feature being worked on right now!</h1>
        </div>
    );
}